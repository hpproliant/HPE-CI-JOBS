- hosts: all
  name: testing job
  tasks:

    - name: Ensure legacy workspace directory
      file:
        path: '/tmp/workspace'
        state: directory

    - shell:
        cmd: |
          set -e
          set -x
          export ANKIT=1
        executable: /bin/bash
        chdir: '/tmp/workspace'
