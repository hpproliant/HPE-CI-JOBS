- job:
    name: test-basic
    description: A job to test prepare-workspace and emit-ara role
    parent: base
    run: playbooks/test-basic.yaml
    nodeset:
      nodes:
        - name: ubuntu
          label: cloud
